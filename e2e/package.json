{
  "name": "mesv2-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "capture:rolls-training": "playwright test tests/rolls-training-capture.spec.ts --project=chromium",
    "build:rolls-training-video": "node scripts/build-rolls-training-video.mjs",
    "make:rolls-training-video": "npm run capture:rolls-training && npm run build:rolls-training-video"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "ffmpeg-static": "^5.3.0"
  }
}
